<div class="center-align">
  <h1><%= current_user.username.capitalize %>, here are your rated restaurants: </h1>

  <% current_user.restaurants.each do |rest| %>
    <li><%= link_to rest.name, restaurant_path(rest.id) %></li>
  <% end %>
</div>
